import { Typography } from "@mui/material"

type Props = Readonly<{}>

export function SupportAdminHome({}: Props) {
    return (
        <Typography variant="h1">Hej support admin</Typography>
    )
}
