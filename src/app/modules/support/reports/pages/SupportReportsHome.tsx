import { Typography } from "@mui/material"

type Props = Readonly<{}>

export function SupportReportsHome({}: Props) {
    return (
        <Typography variant="h1">Hej support report</Typography>
    )
}
